version: '3'

services:
  ignite-gympass-pg:
    image: bitnami/postgresql
    ports:
      - 5432:5432
    environment:
      - POSTGRESQL_USERNAME=italianosebrasileiros
      - POSTGRESQL_PASSWORD=1234
      - POSTGRESQL_DATABASE=onde-tem-lanche
